port: 4222

jetstream {
  store_dir: "/data"
}

# Values are supplied by Compose at runtime. They are deliberately required in
# docker-compose.yml so the broker can never start anonymously.
authorization {
  user: $NATS_USER
  password: $NATS_PASSWORD
}
